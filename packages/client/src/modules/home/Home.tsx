import Feed from "components/feed";
import Sidebar from "components/sidebar";

const HomeModule = () => {
  return (
    <main>
      <Sidebar />
      <Feed />
    </main>
  );
};

export default HomeModule;
